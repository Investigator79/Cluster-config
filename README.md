# Cluster-config
RKE2 Cluster configuration using ansible
This repo contains configuration files/scripts used
install RKE2 K8s engine on a 5 nodes cluster in Microsoft Azure Cloud.

To setup the high availability production grade cluster, a load balancer, a
DNA A record and seperate server that will have Nginx as the proxy is already 
provisioned using terraform.
